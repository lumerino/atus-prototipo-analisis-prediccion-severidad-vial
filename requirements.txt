# Version fijadas (no rangos) a proposito: el modelo persistido
# (modelos/modelo_severidad_histgb.joblib) se serializo con joblib/scikit-learn
# usando estas versiones exactas. joblib.load() de un Pipeline de scikit-learn
# NO es compatible entre versiones distintas de numpy/scikit-learn (falla con
# AttributeError al des-serializar) -- confirmado en el primer deploy en
# Streamlit Community Cloud, que sin estos pines instalo numpy/scikit-learn
# mas recientes y rompio la carga del modelo. Ver tambien runtime.txt (fija
# Python 3.11, la version con la que se entreno).
pandas==1.5.3
numpy==1.24.1
scikit-learn==1.2.1
scipy==1.10.0
joblib==1.2.0
streamlit==1.63.0
matplotlib==3.7.0
altair==6.2.2
# prophet instala cmdstanpy, que en el primer uso descarga el binario de CmdStan
# (requiere conexion a internet una sola vez). Si falla en tu entorno, el
# prototipo sigue funcionando: la seccion "Pronostico agregado" cae de vuelta
# al fallback lineal automaticamente.
prophet==1.4.0
